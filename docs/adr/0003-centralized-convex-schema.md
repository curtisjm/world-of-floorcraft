# Centralized Convex Schema

All Convex table definitions live in `convex/schema.ts`, while domain behavior lives in domain-oriented Convex modules. This accepts global coordination for schema edits in exchange for one generated data model and explicit cross-domain references.
