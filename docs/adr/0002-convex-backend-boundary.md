# Convex Backend Boundary

Convex is the source of truth for application data, backend functions, generated types, and reactive app state. Clerk remains the identity provider and maps into Convex through JWT integration, preserving the existing auth product while replacing the previous Neon, Drizzle, tRPC, and most Ably backend stack.
