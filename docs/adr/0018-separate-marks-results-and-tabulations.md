# Separate Marks Results And Tabulations

Competition scoring stores judge marks separately from computed callback or final results and persisted tabulation displays. This supports correction auditability, recomputation, and a computed-to-reviewed-to-published workflow without recomputing detailed result displays on every read.
