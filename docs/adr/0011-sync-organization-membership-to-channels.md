# Sync Organization Membership To Channels

Org channels are conversations whose access is synchronized from organization membership. Creating, joining, leaving, invite acceptance, and join-request approval update channel membership so org channels behave as member spaces rather than manually curated group chats.
